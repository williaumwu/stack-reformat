def default():

    task = {'method': 'shelloutconfig',
            'metadata': {'env_vars': [],
                         'shelloutconfigs': ['config0-hub:::kafka::create_ansible_replica_hosts']}}

    return task
