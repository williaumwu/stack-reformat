rs.initiate();
sleep(15000);

db.createUser({
  user: "{{mongodb_admin_user}}",
  pwd: "{{mongodb_admin_pass}}",
  roles: [
    {role: "root", db: "admin"}
  ]
});

sleep(3000);

printjson(rs.status());
